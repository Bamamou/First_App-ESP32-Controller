<?xml version="1.0" encoding="utf-8"?>
<full-backup-content>
    <!-- Include all shared preferences by default -->
    <include domain="sharedpref" path="."/>
    <!-- Exclude sensitive data if any exists -->
    <exclude domain="sharedpref" path="sensitive_data.xml"/>
</full-backup-content>